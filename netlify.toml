[dev]
  port = 8888
  publish = "dist"

# Redirect anything that isn't root to `/api`
[[redirects]]
  from = "/*"
  to = "/api/:splat"

[[edge_functions]]
  path = "/api/*"
  function = "github-get-latest"
